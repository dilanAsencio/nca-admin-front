export enum AcademicStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    SUSPENDED = 'suspended'
}

export enum Gender {
    MALE = 'male',
    FEMALE = 'female',
    MIXED = 'mixed'
}

export enum CalendarType {
    A = 'A',
    B = 'B',
    FLEXIBLE = 'Flexible'
}