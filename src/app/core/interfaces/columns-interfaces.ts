export interface LevelColumnsDropTable {
    displayName: string;
    periodoAcademico: string;
    summary: string;
    code: string;
}
export interface GradeLevelChildColumnsDropTable {
    displayName: string;
    maxCapacity: number;
    formattedPrice: string;
}